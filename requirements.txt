# Official ranking dependencies.
#
# rank_ursi_fl.py intentionally uses only the Python standard library. This file
# is kept so sandbox/judge setup can run:
#
#   python -m pip install --no-index -r requirements.txt
#
# without requiring network access or any wheel cache.
